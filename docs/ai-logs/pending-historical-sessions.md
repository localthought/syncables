# Pending historical sessions

Session links that appear as `Claude-Session:` trailers in this repository's
git history but don't yet have a transcript under [`sessions/`](sessions)
would be indexed here. Per
[NLnet's Generative AI policy](https://nlnet.nl/foundation/policies/generativeAI/),
retroactive logging is not required for a project that was already ongoing
before the policy took effect, so this list exists only so a link isn't lost
track of before a transcript becomes available to attach.

None pending currently — every `Claude-Session:` link found in git history has
a matching log under [`sessions/`](sessions).

(Generated by scanning `git log --all --grep=Claude-Session` against full,
unshallowed history. Re-run that if this list needs refreshing against newer
history.)
